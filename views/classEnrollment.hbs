<div class="container" style="padding: 10% 0% 10% 0%">
    <div class="form-group col-3 bg-dark p-3 rounded">
    <form id='filter_people' action='' method=''>
        <label>Filter by Class:</label>
        <select class="form-control" name="classID" id="filter-by-select">
                    <option value="All Classes">All Classes</option>
                    {{#each class}}
                    <option value={{this.cID}}>{{this.name}}</option>
                    {{/each}}
                    </select>
                    <input class="btn btn-primary" type="button" value="Filter" onclick="filterByClass()">
      </form>
    </div>
    <table class="table">
        <thead class="thead-dark">
        <tr>
            <th scope="col">First Name</th>
            <th scope="col">Last Name</th>
            <th scope="col">Class</th>
            <th scope="col">Professor</th>
	        <th scope="col">Drop From Class</th>
        </tr>
        </thead>
        <tbody>
            {{#each student_class}}
            <tr>
                <td>{{fname}}</td>
                <td>{{lname}}</td>
                <td>{{cName}}</td>
                <td>{{professor}}</td>
                <td><button type="button" class="btn btn-danger" onclick="deleteSFromC({{sID}}, {{cID}})">Drop</button></td>
            </tr>
            {{/each}}        
        </tbody>
    </table>
</div>
